# Pages / 프로덕션 빌드와 동일하게 API를 붙이려면:
# 1) 이 파일을 복사해 `frontend/.env.production` 으로 저장합니다. (Git에 올리지 마세요.)
# 2) 아래 값을 Render 대시보드의 웹 서비스 URL로 바꿉니다.
# 3) `npm run build` → 생성된 `dist`가 Pages에 올라가는 결과와 가깝게 확인할 수 있습니다.
#
VITE_API_BASE_URL=https://YOUR-SERVICE.onrender.com
